
- interface 또는 type 사용

```typescript
interface User {
  name: string;
  age: number;
}

type User {
  name: string;
  age: number;
}


const user: User = { name: "Alice", age: 30 };
```

- 유니언(Union | ) & 인터섹션(Intersection & )

```typescript

let value: string | number;
type Combined = { a: string } & { b: number }; // 두 타입 모두 만족 x

```